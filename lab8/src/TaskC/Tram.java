package TaskC;

public class Tram extends PublicTransport{
    public Tram(String routeType, int numberSeats, String flightRegularity) {
        super(routeType, numberSeats, flightRegularity);
    }
}
